import Card from "react-bootstrap/Card";

const Subtitle = () => {
	return (
		<Card.Subtitle>
			<i className="fas fa-circle" /> AppName
		</Card.Subtitle>
	);
};

export default Subtitle;
